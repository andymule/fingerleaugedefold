local M = {}

function init(self)
    -- Set clear color to black
    -- render.set_clear_color(vmath.vector4(0, 0, 0, 1))
end

function update(self)
    -- Clear the screen with black color
    -- render.clear({[render.CLEAR_COLOR_BUFFER_BIT] = true})
end

return M 